[project]
name = "ai-worm"
version = "0.1.0"
description = "C. Elegans. worm artifical clone"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.3.0",
    "numpy>=2.4.6",
    "pygame>=2.6.1",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
